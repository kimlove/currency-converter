// Site configuration settings
// These values normally best in environment variables, but to make it easier getting started, create a config object

export const site = {
  API_BASE_URL: "https://api.currencybeacon.com/v1",
};
